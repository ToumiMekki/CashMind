import React from 'react';
import { WalletScreen } from './WalletScreen';

/**
 * Wallet tab: always normal wallet UI (carousel, quick actions). Business Mode opens as overlay.
 */
export function WalletTabScreen() {
  return <WalletScreen />;
}
